on:
  push:
    branches:
      - main

jobs:
  wait:
    runs-on: ubuntu-latest
    steps:
      - name: Wait for tests to succeed
        uses: lewagon/wait-on-check-action@v1.3.1
        with:
          ref: ${{ github.ref }}
          check-name: "Hello World"
          repo-token: ${{ secrets.WAIT_FOR_TOKEN }}
          wait-interval: 10

      - name: Yay
        run: echo "Hello World succeeded!"
